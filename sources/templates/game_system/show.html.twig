{% extends 'base-page.html.twig' %}

{% block title %}GameSystem{% endblock %}

{% block body2 %}
    <h1>GameSystem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_system.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ game_system.name }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('game_system_index') }}">back to list</a>

    <a href="{{ path('game_system_edit', {'id': game_system.id}) }}">edit</a>

    {{ include('game_system/_delete_form.html.twig') }}

    {% include 'nation/list_group.html.twig' with {'gameSystemId': game_system.id }%}
{% endblock %}
