{% extends 'base.html.twig' %}

{% block title %}World{% endblock %}

{% block body %}
    <h1>World</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ world.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ world.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('world_index') }}">back to list</a>

    <a href="{{ path('world_edit', {'id': world.id}) }}">edit</a>

    {{ include('world/_delete_form.html.twig') }}
{% endblock %}
