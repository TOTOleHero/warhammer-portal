
<div class="dropdown-menu">
{% for world in worlds %}
          <a class="dropdown-item" href="{{ path('world_show', {'id': world.id}) }}">{{ world.name }}</a>
{#          <div class="dropdown-divider"></div> 
          <a class="dropdown-item" href="#">Separated link</a>
#}
{% else %}
    <a class="dropdown-item" href="#">No data</a>
{% endfor %}          
</div>