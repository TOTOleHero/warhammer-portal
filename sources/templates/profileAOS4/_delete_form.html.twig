<form method="post" action="{{ path('profileAOS4_delete', {'id': profileAOS4.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profileAOS4.id) }}">
    <button class="btn">Delete</button>
</form>
