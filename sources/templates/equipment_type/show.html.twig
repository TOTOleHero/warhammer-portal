{% extends 'base.html.twig' %}

{% block title %}EquipmentType{% endblock %}

{% block body %}
    <h1>EquipmentType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipment_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ equipment_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('equipment_type_index') }}">back to list</a>

    <a href="{{ path('equipment_type_edit', {'id': equipment_type.id}) }}">edit</a>

    {{ include('equipment_type/_delete_form.html.twig') }}
{% endblock %}
