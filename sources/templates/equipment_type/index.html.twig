{% extends 'base.html.twig' %}

{% block title %}EquipmentType index{% endblock %}

{% block body %}
    <h1>EquipmentType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for equipment_type in equipment_types %}
            <tr>
                <td>{{ equipment_type.id }}</td>
                <td>{{ equipment_type.name }}</td>
                <td>
                    <a href="{{ path('equipment_type_show', {'id': equipment_type.id}) }}">show</a>
                    <a href="{{ path('equipment_type_edit', {'id': equipment_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('equipment_type_new') }}">Create new</a>
{% endblock %}
