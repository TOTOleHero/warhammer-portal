<form method="post" action="{{ path('profileT9A12_delete', {'id': profileT9A12.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profileT9A12.id) }}">
    <button class="btn">Delete</button>
</form>
